# FluteBand AI — зависимости сервиса для Gradio-пространства Hugging Face (бесплатный тариф).
#
# Устанавливается при сборке пространства. Всё бесплатное и с открытым кодом.
# Системные библиотеки для OpenCV лежат рядом, в packages.txt.

# Сервис-обёртка (те же зависимости, что у Docker-варианта в server/requirements.txt)
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# Gradio нужен потому, что бесплатные пространства Hugging Face бывают только с этим SDK:
# в него монтируется наш FastAPI (см. app.py), маршруты /health, /engines и /recognize не меняются.
gradio>=5.0

# Движок распознавания: ONNX, считает на процессоре, видеокарта не нужна.
# ВЕРСИЯ ЗАКРЕПЛЕНА НАМЕРЕННО. В базовом образе Gradio-пространства Python 3.10, и pip ставит на нём
# homr 0.6.2 (а не 0.7.0, как на Python 3.11 — зависимость rapidocr требует 3.11). Версии ждут
# РАЗНЫЕ файлы моделей: 0.6.2 — encoder/decoder_pytorch_model_331, 0.7.0 — ..._396, у 0.6.2 нет
# флага --gpu. Поэтому в репозитории лежит набор именно для 0.6.2 (models/), а версия закреплена,
# чтобы набор и движок не разъехались. Если менять эту строку — менять и набор моделей:
# он должен соответствовать версии, иначе движок полезет скачивать своё во время запроса.
homr==0.6.2
opencv-python-headless
numpy
requests